# Copy to .env and fill in. NEVER commit the real .env.
# GitHub fine-grained PAT, read-only: Contents: read, Issues: read, Pull requests: read
GITHUB_TOKEN=

# Port the MCP server listens on (defaults to 8080 if unset)
PORT=8080
